const fullName = {
    name: 'Aviram',
    Last_name: 'Eldar',
    fname: function (name) {
        console.log('fname function', name);
    },
};

fullName.fname('timer.js');

export default fullName;